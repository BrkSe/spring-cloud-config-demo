server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BrkSe/spring-cloud-configuration
          default-label: main
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest        